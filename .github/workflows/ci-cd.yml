name: CI/CD Pipeline
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  call-pipeline:
    uses: MorningBlossom/ci-cd/.github/workflows/ci-cd.yml@main
    with:
      go-version: '1.27'